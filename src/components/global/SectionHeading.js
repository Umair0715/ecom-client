import React from 'react'

const SectionHeading = ({ title }) => {
    return (
        <h1 className='text-gradient font-bold text-3xl text-center'>
            {title} 
        </h1>
    )
}

export default SectionHeading